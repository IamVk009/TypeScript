// Creation of typed object which is similer to creating typed variable.
// Creating type of an object
var emp;
// Creating object of above created type.
emp = {
    id: 100,
    name: "Flash"
};
// To access the object properties.
console.log(emp.id);
console.log(emp.name);
